/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RecordMatchResultRequest } from '../../models/record-match-result-request';

export interface RecordMatchResult$Params {
  matchId: string;
  body: RecordMatchResultRequest;
}

export function recordMatchResult(
  http: HttpClient,
  rootUrl: string,
  params: RecordMatchResult$Params,
  context?: HttpContext,
): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, recordMatchResult.PATH, 'put');
  if (params) {
    rb.path('matchId', params.matchId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(rb.build({ responseType: 'text', accept: '*/*', context })).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    }),
  );
}

recordMatchResult.PATH = '/api/matches/{matchId}/result';

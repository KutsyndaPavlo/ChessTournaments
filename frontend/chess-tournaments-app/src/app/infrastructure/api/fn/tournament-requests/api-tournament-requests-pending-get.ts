/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TournamentRequestDto } from '../../models/tournament-request-dto';

export interface ApiTournamentRequestsPendingGet$Params {}

export function apiTournamentRequestsPendingGet(
  http: HttpClient,
  rootUrl: string,
  params?: ApiTournamentRequestsPendingGet$Params,
  context?: HttpContext,
): Observable<StrictHttpResponse<Array<TournamentRequestDto>>> {
  const rb = new RequestBuilder(rootUrl, apiTournamentRequestsPendingGet.PATH, 'get');
  if (params) {
  }

  return http.request(rb.build({ responseType: 'json', accept: 'application/json', context })).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TournamentRequestDto>>;
    }),
  );
}

apiTournamentRequestsPendingGet.PATH = '/api/tournament-requests/pending';
